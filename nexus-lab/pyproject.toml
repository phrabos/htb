[project]
name = "nexus-lab"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "phrabos" }
]
requires-python = ">=3.13"
dependencies = [
    "requests>=2.34.2",
]

[project.scripts]
nexus-lab = "nexus_lab:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
